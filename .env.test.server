# Test Server Environment Configuration for jneconnect.com
NODE_ENV=test
API_VERSION=v1
PORT=8002

# Test Database Configuration - PostgreSQL on same server
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=connectglobal_test
DB_USER=connectglobal_user
DB_PASSWORD=ConnectGlobal2024!Secure#DB
DB_SSL=false

# Test Environment Features
TEST_DATA_ENABLED=true
MATCHING_TEST_USERS=20

# JWT Configuration  
JWT_SECRET=test-jwt-secret-key-for-testing-only-not-production-use
JWT_EXPIRES_IN=24h

# CORS Configuration for test
CORS_ORIGIN=*

# Test Logging
LOG_LEVEL=debug
LOG_TO_FILE=false

# Disable external services for testing
ENABLE_PUSH_NOTIFICATIONS=false
ENABLE_EMAIL_NOTIFICATIONS=false
ENABLE_REDIS=false